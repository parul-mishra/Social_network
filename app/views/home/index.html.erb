<% if signed_in? %>
  <%= render 'posts/form' %>
<% @activities.each do |activity| %>
    <%= render_activity(activity) %>
<% end %>
<% else %>
  <div class="row">
    <aside class="span4">
      <section>
        <%= render 'shared/intro_page' %>
      </section>
  </div>
<% end %>
